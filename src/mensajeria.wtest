import mensajeros.*
describe "mensajes"{
test "peso de neo"{
	neo.peso()
	assert.equals(0,neo.peso())	
	}
test"si puedeLLamar neo"{
	neo.credito(2)
	assert.equals(true,neo.puedeLlamar())
	}
}
test "neo puede entregar" {
	// configurar el paquete
	assert.that(paquete.puedeEntregarsePor(neo))
}